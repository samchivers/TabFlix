// Class for the timespan element
export class Timespan {
    // Selector Id
    public id:number;
    // Timespan value
    public value:number;

    // Construct class
    constructor(id:number, value:number){
        this.id = id;
        this.value = value;
    }
}